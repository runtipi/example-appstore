name: Update & Validate Metadata & Configs of Upgraded Apps

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  # Debug job to log workflow context for troubleshooting
  debug-context:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Debug workflow context
        run: |
          echo "🔍 Debugging workflow execution context"
          echo "Event name: ${{ github.event_name }}"
          echo "Actor: ${{ github.actor }}"
          echo "Repository: ${{ github.repository }}"
          echo "PR number: ${{ github.event.pull_request.number }}"
          echo "PR title: ${{ github.event.pull_request.title }}"
          echo "PR author: ${{ github.event.pull_request.user.login }}"
          echo "Base ref: ${{ github.base_ref }}"
          echo "Head ref: ${{ github.head_ref }}"
          echo "Is Renovate bot: ${{ github.actor == 'renovate[bot]' }}"

  # Job for Renovate PRs - handles automatic updates and merging
  update-and-validate-configs-renovate:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'pull_request' && 
      (github.actor == 'renovate[bot]' || contains(github.event.pull_request.user.login, 'renovate'))
    needs: debug-context
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ssh-key: ${{ secrets.DEPLOY_KEY }}
      
      - name: Check if last commit is config update
        id: check_last_commit
        run: |
          LAST_COMMIT_MSG=$(git log -1 --pretty=format:"%s")
          echo "Last commit message: $LAST_COMMIT_MSG"
          if [[ "$LAST_COMMIT_MSG" == *"ci: update config.json after renovate docker-compose update"* ]]; then
            echo "🔄 Skipping workflow - last commit is a config update"
            echo "skip_workflow=true" >> $GITHUB_OUTPUT
          else
            echo "✅ Proceeding with workflow - last commit is not a config update"
            echo "skip_workflow=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Get list of apps changed in the PR
        if: steps.check_last_commit.outputs.skip_workflow == 'false'
        id: changed_apps
        uses: tj-actions/changed-files@v46
        with:
          files: apps/*/docker-compose.json

      - name: Backup previous config.json tipi_version
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          pwsh ./.github/scripts/backup-config.ps1

      - name: Update config.json only for changed apps (PR Renovate)
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          pwsh ./.github/scripts/update-config.ps1

      - name: Check config.json update consistency
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          pwsh ./.github/scripts/validate-config.ps1

      - name: Commit and push config.json updates
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          GITHUB_HEAD_REF: ${{ github.head_ref }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          pwsh ./.github/scripts/commit-and-push-configs.ps1

      - name: Check if update is major version
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true'
        id: check_major
        shell: pwsh
        env:
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          pwsh ./.github/scripts/check-major-version.ps1 -PrBody $env:PR_BODY

      - name: Add approved-to-merge label to PR
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true' && steps.check_major.outputs.is_major == 'false' && success()
        run: |
          gh pr edit ${{ github.event.pull_request.number }} --add-label "approved-to-merge"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable automerge for approved PR
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true' && steps.check_major.outputs.is_major == 'false' && success()
        run: |
          if ! gh pr merge --squash --auto ${{ github.event.pull_request.number }}; then
            echo "⚠️ Auto-merge failed - likely due to branch protection rules not configured for auto-merge"
            echo "✅ PR is approved and ready for manual merge"
            exit 0
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Skip automerge for major updates
        if: steps.check_last_commit.outputs.skip_workflow == 'false' && steps.changed_apps.outputs.any_changed == 'true' && steps.check_major.outputs.is_major == 'true'
        run: |
          echo "🚫 Major version update detected - manual review required"
          echo "::warning::This PR contains a major version update and requires manual review before merging"
          gh pr edit ${{ github.event.pull_request.number }} --add-label "major-update" --add-label "needs-review"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Job for manual contributions - validates configurations only
  validate-manual-changes:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && !(github.actor == 'renovate[bot]' || contains(github.event.pull_request.user.login, 'renovate'))
    needs: debug-context
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Get list of apps changed in the PR
        id: changed_apps
        uses: tj-actions/changed-files@v46
        with:
          files: |
            apps/*/docker-compose.json
            apps/*/config.json
            apps/*/metadata/**

      - name: Validate JSON syntax
        if: steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          pwsh ./.github/scripts/validate-json-syntax.ps1

      - name: Validate config.json structure
        if: steps.changed_apps.outputs.any_changed == 'true'
        shell: pwsh
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          pwsh ./.github/scripts/validate-config-structure.ps1

      - name: Test Docker Compose validity
        if: steps.changed_apps.outputs.any_changed == 'true'
        shell: bash
        env:
          CHANGED_FILES: ${{ steps.changed_apps.outputs.all_changed_files }}
        run: |
          bash ./.github/scripts/validate-docker-compose.sh

      - name: Validation Summary
        if: always()
        run: |
          echo "📋 Configuration validation completed for PR #${{ github.event.pull_request.number }}"
          echo "🔍 Changed files: ${{ steps.changed_apps.outputs.all_changed_files }}"
          echo "ℹ️  This PR requires manual review as it's not from Renovate Bot"
